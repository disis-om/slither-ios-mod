# Build output - rebuild with Scripts/repack-ipa.ps1 or the CI workflow.
dist/

# Regenerate with Scripts/export-assets.ps1; 35 MB of PNGs that nothing reads
# at runtime, so they stay out of the repo.
assets/

# IPAs: the input this tree was unpacked from, and any build downloaded
# from CI. Both stay local.
*.ipa
SHA256SUMS

# Local scratch
work/
*.log
